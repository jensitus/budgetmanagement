<%= form_with(model: @household, url: { action: 'update'}, local: true, remote: true) do |form| %>

    <div class="row">
      <div class="col-md-6">
        <%= select_tag 'add_user', options_from_collection_for_select(@users, 'id', 'username'), class: "form-control", include_blank: true %>
          <option></option>
          <%# @users.each do |u| %>
              <option><%#= u.username %></option>
          <%# end %>
        <!-- </select> -->
      </div>
      <%= form.hidden_field :name %>
      <div class="col-md-6">
        <div class="actions">
          <%= form.submit :add_user, class: 'btn btn-outline-primary' %>
        </div>
      </div>
    </div>

<% end %>